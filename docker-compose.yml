services:
  kira_caster:
    build: .
    ports:
      - "${KIRA_ADMIN_PORT:-8080}:8080"
    volumes:
      - kira_data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/status"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 15s

volumes:
  kira_data:
